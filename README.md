# El_Cid_Members
This App will open XLS and CSV if using the  index-GithubCSV.html
However, if you want to edit the XLS from withing Github, you cannot.
NOTE: The XLS file downloaded from golf group manager is in a state that you cannot create a CSV,
you have to copy the data and paste into new workbook and then create the csv and upload it to github repository
UPDATE: the app now opens up the file from my google Drive account.  Much safer this way.
I use Google Sheets now to edit and it will automatically update.
